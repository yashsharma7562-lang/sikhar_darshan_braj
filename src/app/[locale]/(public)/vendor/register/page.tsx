export default function VendorRegisterPage() {
  return (
    <section className="container-shell py-12 md:py-20">
      <div className="bg-peacock-deep mx-auto max-w-4xl rounded-3xl p-8 text-white md:p-12">
        <p className="text-soft-gold text-sm font-bold tracking-widest uppercase">
          Vendor registration
        </p>
        <h1 className="mt-4 text-4xl font-black">
          Join only through verification.
        </h1>
        <p className="mt-5 text-white/80">
          Hotels, dharamshalas, transport operators and local service providers
          can apply. Submission never creates a verified badge, temple
          partnership, active listing or payout entitlement.
        </p>
        <div className="mt-8 rounded-2xl bg-white/10 p-5 text-sm">
          Application storage and document review are not configured, so
          registration is currently unavailable and no data will be accepted.
        </div>
      </div>
    </section>
  );
}
